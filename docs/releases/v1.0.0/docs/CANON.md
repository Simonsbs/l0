# Canonicalization Rules (Bootstrap)

Current enforced canonical form:
- exact keyword spelling
- fixed section order
- single spaces where required
- newline-delimited sections
- explicit empty sections allowed and expected

Future extension:
- stable sorting of IDs
- canonical function and block formatting
- canonical instruction token spacing
